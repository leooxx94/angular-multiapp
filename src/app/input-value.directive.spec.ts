import { InputValueDirective } from './input-value.directive';

describe('InputValueDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValueDirective();
    expect(directive).toBeTruthy();
  });
});
